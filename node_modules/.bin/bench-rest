#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bench-rest/bin/bench-rest" "$@"
  ret=$?
else 
  node  "$basedir/../bench-rest/bin/bench-rest" "$@"
  ret=$?
fi
exit $ret
